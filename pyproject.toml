[project]
name = "learning-python-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mark", email = "mark_55rus@mail.ru" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
learning-python-2 = "learning_python_2:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
