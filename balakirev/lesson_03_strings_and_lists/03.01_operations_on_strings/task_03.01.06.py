# Подвиг 4. На вход программе подаются две строки (каждая вводится с новой строки). Необходимо их прочитать командами:

# s1 = input() # чтение первой строки
# s2 = input() # чтение второй строки

# и объединить в одну строку через пробел. Результат вывести на экран.

s1 = input() # чтение первой строки
s2 = input() # чтение второй строки
print(s1, s2)
